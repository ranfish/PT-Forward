{
  "id": "2e2b10d7",
  "alias": "收益1",
  "minUploadSpeedUnit": "MiB",
  "maxProgress": "0.1",
  "minRatio": "0.2",
  "minDownloadSpeedUnit": "MiB",
  "excludeCategory": "不删",
  "type": "javascript",
  "code": "// 分享率\n(maindata, torrent) => {\n  const categoryList = [\"不删\"];\n  const ruleData = [\n    { progress: 0.4, ratio: 0.7 },\n    { progress: 0.3, ratio: 0.6 },\n    { progress: 0.2, ratio: 0.35 },\n    { progress: 0.1, ratio: 0.2 },\n    { progress: 0.05, ratio: 0.1 }\n  ];\n  const { state, upspeed, category, progress, ratio } = torrent.originProp;\n  if (categoryList.indexOf(category) !== -1) {\n    return false;\n  }\n  for (const rule of ruleData) {\n    if (\n      state == \"downloading\" &&\n      progress >= rule.progress &&\n      ratio <= rule.ratio &&\n      upspeed <= util.calSize(6, \"MiB\")\n    ) {\n      return true;\n    }\n  }\n  return false;\n};",
  "used": true
}