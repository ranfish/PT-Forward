services:
  pt-forward:
    image: ghcr.io/ranfish/pt-forward:latest
    container_name: pt-forward
    ports:
      - "8765:8765"
    volumes:
      - ./data:/data
      - ./config:/config
      - ./logs:/logs
    restart: unless-stopped
